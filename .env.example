# HIVEMND_TOKEN overrides the macOS Keychain for ephemeral/headless sessions.
HIVEMND_TOKEN=
# Optional. Defaults to ~/.hivemnd/config.json.
HIVEMND_CONFIG=
# Optional. Defaults to ~/.hivemnd and owns local non-secret sync state.
HIVEMND_HOME=
